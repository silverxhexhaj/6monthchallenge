export function Footer() {
  return (
    <footer className="border-t border-border px-8 py-12 text-center text-sm tracking-[1px] text-muted">
      <p>
        Inspired by Jim Rohn — &quot;You are the master of your mind, not the
        other way around.&quot;
      </p>
    </footer>
  );
}
